cmake_minimum_required(VERSION 2.8)
project( TwoPlayerTetris )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( TwoPlayerTetris main.cpp )
target_link_libraries( TwoPlayerTetris ${OpenCV_LIBS} )